1. 통합 예외처리
2. 문자열 함수
3. 날짜 처리
4. 숫자처리

- DB연결처리 (helper, DBCP커넥션풀 등)
- 이클립스 환경 Lib : DB연결(오라클.jar), 롬복, JSTL(자카르타 서블릿2개), 레포팅, 파일업로드
- 실제로는 jar 라이브러리까지 배포할 필요가 없으므로 인터넷이 되는 환경에서 빌드 배포도구인 maven이나 gradle으로 설정하는 것이 현명하다.



